# 구현할 기능 목록

-   CardUI Component

    -   [x] Input 컴포넌트의 카드 번호와 상태 값 공유
    -   [x] Input 컴포넌트의 날짜와 상태 값 공유
    -   [x] 카드 브랜드 구분 로직으로 브랜도 로고 렌더링

-   Input Component

    -   [x] 유효하지 않은 번호 입력 시 피드백 제공
        -   [x] 1~12사이 숫자를 입력해주세요.
        -   [x] 숫자만 입력 가능합니다.
    -   [x] type 별 UI(카드 번호, 유효기간, CVC)
    -   [x] type 별 유효 숫자 범위
        -   [x] card-number: 4글자까지
        -   [x] exp-month: 2글자까지, 1에서 12 사이
        -   [x] cvc: 3글자까지
    -   유효성 검사를 Input 컴포넌트에서 알 필요가 없는 것 같아서 Input은 value 관리 및 스타일만 담당하고 유효성 검사는 외부(InputWrapper)에서 실시하는 걸로

-   카드 브랜드 구분 로직

    -   [x] Visa: 4로 시작하는 16자리 숫자
    -   [x] MasterCard: 51~55로 시작하는 16자리 숫자

-   CardNumber Section Component

-   EXP Section Component

-   CVC Section Component

# 중간 리팩토링 목록

-   [x] is..Number 유틸들 -> 통합 처리
-   [x] CardInfoInputProps 인터페이스에서 타입안에서 타입을 구분하기 위한 용도로 사용한 식별 유니온 패턴 제거
-   [x] 커링, useReducer 등의 상태 관리 방식 적용하여 App.tsx의 상태 단순화
-   [x] CardInputWrapper 컴포넌트 children 렌더링 방식 map 활용하여 단순화
-   [x] 각 CardInfoInput 컴포넌트에서 길이 미달 시 border 색 변경하기 위한 유효성 검사 유틸 추가
-   [x] 최대 길이 제한 -> 입력 못하게 하는 함수로 관리
-   [x] focus된 input border 색상 변경
-   [x] 문자열 입력에 대해서 어떻게 처리할지?
    -   입력 아예 안되게 하고 피드백도 안 주기(지금)
    -   입력 되고 피드백 주기
    -   입력 안 되고 피드백만 주기
        -   피드백은 어떻게 줄건데? -> 빨간 박스만?(진짜 아니겠다 싶으면)
        -   현재 구조에서 errorMessage를 InputWrapper에서 제공하는데(에러 메세지 렌더링 위치 때문에)
            -   이 구조 때문에 각 Input에서 한글자 입력할 때마다 value를 판단해서 에러 메세지를 제공하기 어려운 구조임
            1. Wrapper를 사용하는 `CardNumberWrapper` `EXPWrapper` `CVCWrapper`를 만들고 그 컴포넌트 안에서 각각 isError, setIsError를 만들고 setIsError를 Input 컴포넌트에 전달하고 validate 통과 실패 시 setIsError 호출하도록
