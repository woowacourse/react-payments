cd "$(git rev-parse --show-toplevel)"

if git diff --cached --name-only | grep -q "^app/src/"; then
  cd "$(git rev-parse --show-toplevel)/app"
  npm run lint
  npm run test-unit
  npm run test-storybook
fi
