## 요구사항 분석

### 카드 등록 페이지

- 등록 버튼 클릭 시 POST /cards로 카드 정보를 전송한다.
  - 201 응답 수신 시 카드 목록 페이지(/cards)로 이동한다.
  - 서버가 400 응답을 반환하면 필드 아래에 메시지를 표시한다.
    - code를 해당 입력 필드로 매핑하기

### 카드 목록 페이지 (/cards)

- 페이지 진입 시 GET /cards로 등록된 카드 목록 조회
- 비동기 상태(idle | loading | success | error)에 맞는 UI를 표시한다.
  - loading: 스피너 또는 스켈레톤
  - success + 목록이 비어 있음: "등록된 카드가 없습니다" 안내
  - success + 목록이 있음: 카드 프리뷰 리스트
  - error: 에러 메시지와 재시도 버튼

- 각 카드에 삭제 버튼 클릭시
  - window.confirm()으로 확인을 받는다.
  - 확인 시 `DELETE /cards/:id`로 카드 삭제 요청을 보낸다.
    - 삭제 성공 시 카드 목록을 새로고침한다.
  - 취소 시 아무 동작도 하지 않는다.

## 구현 사항

- [x] msw 환경 구축

### 카드 등록 페이지

- [x] 등록 버튼 클릭 시 `POST /cards`로 카드 정보를 전송한다.
  - [x] `POST /cards` msw 핸들러 구현
  - [x] 201 응답 수신 시 카드 목록 페이지(`/cards`)로 이동한다.
  - [ ] 서버가 400 응답을 반환하면 각 입력 필드 아래에 메시지를 표시한다.
    - [x] code를 해당 입력 필드로 매핑하기

### 카드 목록 페이지 (/cards)

- [x] `/cards` 경로에 카드 목록 페이지 컴포넌트 라우팅 매핑
- [ ] 카드 목록 페이지 ui 구현
  - [x] 카드 목록 페이지 헤더 구현
  - [x] 카드 스캘레톤 ui 구현
  - [ ] 빈 카드 목록 ui 구현
  - [x] 카드 리스트 ui 구현
  - [ ] 카드 에러 ui 구현
- [ ] 페이지 진입 시 GET /cards로 등록된 카드 목록 조회
  - [x] `GET /cards` msw 핸들러 구현
- [ ] 비동기 상태(idle | loading | success | error)에 맞는 UI를 표시한다.
  - [ ] loading: 스피너 또는 스켈레톤
  - [ ] success + 목록이 비어 있음: "등록된 카드가 없습니다" 안내
  - [ ] success + 목록이 있음: 카드 프리뷰 리스트
  - [ ] error: 에러 메시지와 재시도 버튼

- [ ] 각 카드에 삭제 버튼 클릭시 아래의 동작을 한다.
  - [ ] window.confirm()으로 확인을 받는다.
  - [ ] 확인 시 `DELETE /cards/:id`로 카드 삭제 요청을 보낸다.
    - [x] `DELETE /cards/:id` msw 핸들러 구현
    - [ ] 삭제 성공 시 카드 목록을 새로고침한다.
  - [ ] 취소 시 아무 동작도 하지 않는다.
