## 라우팅 구조

| 경로              | 페이지           |
| ----------------- | ---------------- |
| `/cards`          | 카드 목록 페이지 |
| `/cards/register` | 카드 등록 페이지 |

---

## 기능 명세

### 1. 카드 목록 (`/cards`)

페이지 진입 시 `GET /cards`로 등록된 카드 목록을 조회한다.

비동기 상태에 따라 아래와 같이 UI를 표시한다.

#### 1-1. loading

- 응답 대기 중에는 스피너를 표시한다.
- 스켈레톤 대신 스피너를 사용하는 이유: 불러올 카드 개수를 미리 알 수 없기 때문이다.

#### 1-2. success

- **목록이 비어 있음**: "등록된 카드가 없습니다" 안내 문구를 표시한다.
- **목록이 있음**: 카드 프리뷰 리스트를 표시한다.
- 카드 추가 버튼 클릭 시 `/cards/register`로 이동한다.

#### 1-3. error

- "카드 목록을 불러올 수 없습니다" 에러 메시지를 표시한다.
- 다시 시도 버튼 클릭 시 `/cards`로 이동하여 재조회한다.

> **idle 상태인 경우**
> 페이지 진입 즉시 요청을 시작하는 구조이므로 idle 상태는 별도로 처리하지 않는다. 지금 상황에서는 필요가 없다고 느꼈다.

---

### 2. 카드 등록 (`/cards/register`)

- 등록 버튼 클릭 시 `POST /cards`로 카드 정보를 전송한다.
- 서버 응답이 오기 전까지 등록 버튼을 비활성화하고 로딩 스피너를 표시한다.

#### 서버 응답 처리

| 상태 코드 | 처리                                                                   |
| --------- | ---------------------------------------------------------------------- |
| `201`     | 카드 목록 페이지(`/cards`)로 이동                                      |
| `400`     | 응답의 `code`를 해당 입력 필드로 매핑하여 필드 하단에 에러 메시지 표시 |
| 기타 오류 | "카드 등록에 실패했습니다" alert 표시 후 등록 버튼 재활성화            |

---

### 3. 카드 삭제

- 각 카드에 삭제 버튼을 제공한다.
- 삭제 버튼 클릭 시 `window.confirm()`으로 사용자 확인을 받는다.
  - **확인**: `DELETE /cards/:id` 요청 후 목록을 갱신한다.
  - **취소**: 아무 요청도 보내지 않는다.

---

## BIN 룩업 참고

| 구분                              | 식별 주체                                  |
| --------------------------------- | ------------------------------------------ |
| 국제 브랜드 (Visa, Mastercard 등) | 클라이언트에서도 앞자리 패턴으로 판별 가능 |
| 발급사 (신한, 국민, 우리 등)      | 서버 BIN 룩업 필요                         |

---

## MSW 핸들러

| 메서드   | 경로         | 시나리오                           |
| -------- | ------------ | ---------------------------------- |
| `POST`   | `/cards`     | 201 정상 응답                      |
| `POST`   | `/cards`     | 400 에러 (지원하지 않는 카드사 등) |
| `GET`    | `/cards`     | 카드 목록 응답                     |
| `DELETE` | `/cards/:id` | 삭제 성공 응답                     |

---

## 테스트 시나리오

- 카드 등록 성공 시 `/cards`로 이동하는 통합 테스트
- 카드 등록 실패(400) 시 필드별 에러 메시지 표시 테스트
- 카드 목록 `loading` / `success` / `error` 상태별 UI 테스트
- 빈 목록일 때 안내 문구 표시 테스트
- 삭제 확인 시 `DELETE` 요청 후 목록 갱신 테스트
- 삭제 취소 시 요청 미발생 테스트
