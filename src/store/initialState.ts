import { State } from './type';

const initialState: State = {
  cardNumbers: ['', '', '', ''],
  cardExpirationDate: ['', ''],
  cardOwner: [''],
  cardCVC: [''],
  cardPWD: ['', ''],
};

export default initialState;
