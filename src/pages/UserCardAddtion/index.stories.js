import React from 'react';
import { UserCardAddition } from '.';

export default {
  title: 'Pages/UserCardAddition',
  component: UserCardAddition,
};

const Template = (args) => <UserCardAddition {...args} />;

export const Default = Template.bind({});
