import React from 'react';
import SecurityCodeGuide from '.';

export default {
  title: 'Page/AddCard/SecurityCodeGuide',
  component: SecurityCodeGuide,
};

const Template = (args) => <SecurityCodeGuide {...args} />;

export const Default = Template.bind({});

Default.args = {};
