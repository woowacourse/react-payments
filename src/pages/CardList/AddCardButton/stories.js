import React from 'react';
import AddCardButton from '.';

export default {
  title: 'Page/CardList/AddCardButton',
  component: AddCardButton,
};

const Template = (args) => <AddCardButton {...args} />;

export const Default = Template.bind({});

Default.args = {};
