import React from 'react';
import { AddCardPages } from '.';

export default {
  title: 'Pages/AddCardPages',
  component: AddCardPages,
};

const Template = (args) => <AddCardPages {...args} />;

export const Default = Template.bind({});
