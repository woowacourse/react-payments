import Styled from 'styled-components';
import { CreditCardContainer } from '../../components/CreditCard/styles';

export const CreditCardInputTracker = Styled(CreditCardContainer)`
  margin: 0 auto;
`;
