const color = {
  primary: '#ACACAC',
  dark: '#000000',
  error: '#FF3D3D',
};

const fontSize = {
  sm: '10px',
  base: '12px',
  md: '14px',
  lg: '18px',
};

const theme = {
  color,
  fontSize,
};

export default theme;
