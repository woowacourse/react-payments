import React from 'react';

export const CardInfoContext = React.createContext(null);
export const CardListDispatchContext = React.createContext(null);
export const CardInfoDispatchContext = React.createContext(null);

export const PathContext = React.createContext('add-card');
