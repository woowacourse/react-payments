import React from 'react';

export const CardInfoContext = React.createContext(null);
export const CardDispatchContext = React.createContext(null);

export const PathContext = React.createContext('add-card');
