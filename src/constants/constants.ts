export const HIDDEN_ELEMENT_STYLE = `
  visibility: hidden;
  opacity: 0; 
  pointer-events: none;
`;

export const LENGTH = {
  EACH_CARD_NUMBER: 4,
  EXPIRATION: 2,
  NAME: 20,
  EACH_PASSWORD: 1,
  SECURITY_CODE: 3,
};

export const REGEX = {
  ONLY_NUMBER: /[^\d]/g,
  ONLY_ENGLISH: /[^a-z A-Z]/g,
};
