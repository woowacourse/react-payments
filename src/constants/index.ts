const LOCAL_STORAGE_KEY = "cardList";

const SECURITY_TEXT_ICON = "•";

const REGEX = {
  ONLY_NUMBER: /^\d*$/,
  ENGLISH_AND_WHITESPACE: /^[a-zA-Z ]*$/,
  DATE_FORMAT: /^(?:\d{0,2}\/?\d{0,2})?$/,
};

export { LOCAL_STORAGE_KEY, SECURITY_TEXT_ICON, REGEX };
