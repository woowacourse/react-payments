import styled from "styled-components";

const St = {
  Page: styled.section`
    display: flex;
    flex-direction: column;
    gap: 25px;

    width: 100vw;
    height: 100vh;

    padding: 28px;
  `,
};

export default St;
