import React from "react";

import CardPassword from "../components/add/CardInfoForm/CardPassword";

export default {
  component: CardPassword,
  title: "CardPassword",
};

const Template = args => <CardPassword {...args} />;

export const Default = Template.bind({});
