import React from "react";

import CardSecurityCode from "../components/add/CardInfoForm/CardSecurityCode";

export default {
  component: CardSecurityCode,
  title: "CardSecurityCode",
};

const Template = args => <CardSecurityCode {...args} />;

export const Default = Template.bind({});
