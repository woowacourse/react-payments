import React from 'react';

import App from '../App';

export default {
  title: 'Example/App',
  component: App,
};

function Template() {
  return <App />;
}

export const Default = Template.bind({});
