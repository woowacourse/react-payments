import React from 'react';

import App from '../App';

export default {
  title: 'Example/App',
  component: App,
};

const Template = () => <App />;

export const Default = Template.bind({});
