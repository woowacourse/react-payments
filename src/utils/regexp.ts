export const ONLY_NUMBER_REGEXP = /\d/g;
export const ONLY_MONTH_REGEXP = /^(1[0-2]|[1-9])$/;
export const ONLY_ENG_AND_EMPTY_REGEXP = /^[a-zA-Z][a-zA-Z ]*$/;
export const CONTINUOUS_EMPTY_REGEXP = /[ ]{2,}/;
