import styled from 'styled-components';

import Input from '../../../components/Input';

const CardOwnerInputStyled = styled(Input)`
  text-align: left;
  padding-left: 30px;
`;

export default CardOwnerInputStyled;
