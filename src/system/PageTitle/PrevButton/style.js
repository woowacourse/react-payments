import styled from 'styled-components';

const PrevbuttonStyled = styled.span`
  cursor: pointer;
  
  &:hover {
    color: var(--card-chip-background-color);
  }
`;

export default PrevbuttonStyled;
