import styled from 'styled-components';

const TibButtonStyled = styled.div`
  width: 27px;
  height: 27px;
  margin-left: 15px;
  background: var(--tip-button-background-color);
  border: 1px solid var(--tip-button-border-color);
  border-radius: 100%;
  box-sizing: border-box;
  text-align: center;
  line-height: 27px;
  color: var(--tip-button-text-color);
  font-size: 20px;
`;

export default TibButtonStyled;
