import PossessCard from "./PossessCard";

export default {
  title: "PossessCard",
  component: PossessCard,
};

const Template = (args) => <PossessCard {...args} />;

export const PossessCardTemplate = Template.bind({});
