import AddCardPage from '.';

export default {
  component: AddCardPage,
  title: 'AddCardPage/AddCardPage',
};

const Template = args => <AddCardPage {...args} />;

export const Default = Template.bind({});
