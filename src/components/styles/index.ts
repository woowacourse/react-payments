import styled from 'styled-components';

export const SubmitButton = styled.button`
  position: absolute;

  bottom: 0;
  right: 0;

  font-size: 16px;
  font-weight: 900;

  &:focus {
    padding: 10px 20px;
  }
`;
