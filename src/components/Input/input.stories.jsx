import React from 'react';
import { BlockInput, UnderlineInput } from '.';

export default {
  title: 'Payment/Input',
};

export const BlockedInputExample = args => <BlockInput {...args} />;

export const UnderlineInputExample = args => <UnderlineInput {...args} />;
