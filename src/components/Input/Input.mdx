import { Meta, Title, Stories, Controls } from "@storybook/addon-docs";
import InputStories from "./Input.stories";
import Input from "./Input";

<Title>Input</Title>

이 컴포넌트는 사용자가 텍스트를 입력할 수 있는 필드를 제공합니다. 다양한 속성을 통해 입력 필드의 모양과 동작을 조정할 수 있습니다.

<Meta of={InputStories} />

<Stories />

<Controls />
