import styled from 'styled-components';

const Button = styled.button.attrs({
  type: 'submit',
})`
  color: #04c09e;
  background: transparent;
  border: none;
  font-weight: bold;
  font-size: 1rem;
`;

export { Button };
