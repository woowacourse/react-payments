import PrevButton from 'components/PrevButton/PrevButton';

export default {
  title: 'components/PrevButton',
  component: PrevButton,
};

const Template = (args) => <PrevButton {...args} />;

export const Example = Template.bind({});
