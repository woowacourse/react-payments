import React from 'react';
import Tooltip from '.';

export default {
  title: 'CardAddPage/Tooltip',
  component: Tooltip,
};

const Template = (args) => <Tooltip {...args} />;

export const TooltipImage = Template.bind({});
TooltipImage.args = {};
