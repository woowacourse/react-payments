import styled from '@emotion/styled';

const Styled = {
  Container: styled.p`
    width: 100%;
    color: red;
    display: block;
    position: absolute;
    font-size: 12px;
    min-height: 1em;
    margin: 0;
  `,
};

export default Styled;
