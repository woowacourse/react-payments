import React from 'react';
import PrevPageButton from './PrevPageButton';

export default {
  title: 'PrevPageButton',
  component: PrevPageButton,
};

const Template = (args) => <PrevPageButton {...args} />;

export const Default = Template.bind({});
