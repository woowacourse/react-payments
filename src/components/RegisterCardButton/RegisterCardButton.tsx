import styles from "./RegisterCardButton.module.css"

const RegisterCardButton = () => {
    return (
        <button className={styles.registerCardButton}>확인</button>
    );
}

export default RegisterCardButton;