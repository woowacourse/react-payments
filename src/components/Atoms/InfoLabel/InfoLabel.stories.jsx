import React from 'react';
import InfoLabel from '.';

export default {
  title: 'Payment/Atoms/InfoLabel',
  component: InfoLabel,
};

const Template = args => <InfoLabel {...args} />;

export const Default = Template.bind({});

Default.args = {};
