import React from 'react';
import PlusCard from 'components/Card/PlusCard';

export default {
  title: 'PlusCard',
  component: PlusCard,
};

const Template = () => <PlusCard />;

export const Example = Template.bind({});
