import * as styled from './InputContainer.styled';

const InputContainer = (props: any) => {
  return (
    <styled.InputContainer>
      <styled.Inputs></styled.Inputs>
    </styled.InputContainer>
  );
};

export default InputContainer;
