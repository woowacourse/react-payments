import styles from "./SubmitButton.module.css";

const SubmitButton = () => {
  return (
    <button className={styles.container} autoFocus>
      확인
    </button>
  );
};

export default SubmitButton;
