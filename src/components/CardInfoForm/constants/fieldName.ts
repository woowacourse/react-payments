export const CARD_NUMBER_FIELDS = [
  'first',
  'dash',
  'second',
  'dash',
  'third',
  'dash',
  'fourth',
] as const;

export const EXPIRED_DATE_FIELDS = ['month', 'slash', 'year'] as const;

export const PASSWORD_FIELDS = ['first', 'second', 'secret', 'secret'] as const;
