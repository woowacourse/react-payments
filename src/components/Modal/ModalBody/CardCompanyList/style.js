import styled from 'styled-components';

const Container = styled.div`
  width: 75%;
  height: 70%;
  padding: 0.25rem;
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 0.75rem 0.5rem;
  margin: 0;
`;

export { Container };
