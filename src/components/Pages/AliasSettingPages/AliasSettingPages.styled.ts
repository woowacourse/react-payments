import styled from 'styled-components';

import {
  CardInfoSubmitButton,
  CardInfoSubmitButtonContainer,
} from '../../CardInfoSubmitButtonContainer/CardInfoSubmitButtonContainer.styled';

export const AliasSettingPages = styled.section``;

export const AliasSubmitButtonContainer = styled(CardInfoSubmitButtonContainer)``;

export const AliasSubmitButton = styled(CardInfoSubmitButton)``;
