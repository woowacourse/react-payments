### step2를 하면서 가져갈 키워드 & 목표

#### - 커스텀 훅을 이용하여 책임을 분리한다.

- custom hooks 사용법을 익힌다.
- 하나의 컴포넌트는 하나의 일만 하도록 응집도를 높인다.
- UI와 관련 없는 도메인 로직은 custom hooks로 분리한다.

---

### step2 액션 플랜

- 액션 플랜 : 카드 폼 페이지 컴포넌트에서 useState를 직접 쓰지 않는다. 카드 번호/CVC/만료일/카드사 상태와 각각의 onChange 핸들러가 모두 훅 안에 존재한다.
- 측정 방법 : 카드 폼 페이지 컴포넌트 파일에 useState import가 없고, useCardForm이 상태값과 핸들러를 반환하는 구조인지 확인하다.

---

### step1의 개선점

- [ ] 카드 번호 입력창 `data-index` dataset 대신 다른 방식으로 index 전달
- [ ] 각 입력 컴포넌트의 validation 규칙을 별도 파일/상수로 분리
- [ ] `validateMonth`, `validateYear`, `validateCVC`의 불필요한 배열 생성 제거 후 숫자 비교로 교체
- [ ] `useCardBrand`를 Hook이 아닌 순수 함수로 전환하여 `utils.ts`로 이동
- [ ] `CardCVCInput`의 `onChange`를 `ChangeEvent` 대신 값(`string`) 전달 방식으로 통일

---

### 프로그래밍 요구사항

- [ ] react-router를 이용하여 페이지를 관리한다.
- [ ] Custom Hook을 활용하여 Form 관리 로직을 UI와 분리한다.
- [ ] 처음부터 훅 구조를 설계하지 않는다.
- [ ] 각 스토리에 명확한 이름을 지정하여 컴포넌트의 사용 사례를 쉽게 이해할 수 있도록 한다.

---

### 기능 목록

집중해야 할 기능 목록부터 우선순위를 정했습니다.

1. 카드사 선택

- [ ] select 컴포넌트에서 카드사를 선택할 수 있다.
- [ ] 선택한 카드사에 따라 프리뷰 카드의 색상이 변경된다.

2. cvc 번호

- [ ] 숫자만 입력 가능하며, 유효하지 않은 값 입력 시 피드백을 제공한다.

3. 폼 제출 및 상태 관리

- [ ] 모든 카드 정보가 유효하게 입력되었을 때만 확인 버튼이 활성화된다.
- [ ] 입력값이 하나라도 유효하지 않으면 확인 버튼이 비활성화된다.

4. 카드 등록 완료 페이지

- [ ] 카드 등록 완료 메시지를 표시한다.
- [ ] 카드 등록 페이지로 돌아갈 수 있는 확인 버튼을 제공한다.
- [ ] 확인 버튼을 누르면 카드 등록 페이지로 이동하며, 기존에 입력한 값이 유지된다.

5. 다양한 카드사 지원

- [ ] 카드 브랜별 식별 번호 및 카드 번호 유효성 검사를 구현한다.
  - [ ] 입력한 첫 번째 숫자에 따라 브랜드를 판별하고, 브랜드별 길이 기준으로 유효성을 검사한다.
  - [ ] Diners: 36으로 시작하는 14자리 숫자
    - 예시: 3612 345678 9012
      AMEX: 34, 37로 시작하는 15자리 숫자
    - 예시 (34로 시작): 3412 345678 90123
    - 예시 (37로 시작): 3712 345678 90123
      유니온페이: 카드의 앞 번호가 아래 3가지 조건을 만족하는 16자리 숫자
    - 622126~622925로 시작하는 경우: 6221 2612 3456 7890
    - 624~626로 시작하는 경우: 6240 1234 5678 9012
    - 6282~6288로 시작하는 경우: 6282 1234 5678 9012

6.  동적 입력 UI 구현

- [ ] 이전 입력이 완료된 후 다음 입력 컴포넌트가 나타난다.
- [ ] 유효하지 않은 값이 입력된 경우 다음 컴포넌트가 나타나지 않는다.
- [ ] 입력 완료 후 다음 필드로 포커스가 자동 이동한다.
