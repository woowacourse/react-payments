# Trade-off 고민 과정 정리

## 1. Error Status 관리 주체 (Page vs Field)

| 비교 항목  | Page(Form)에서 관리 (선택)                         | Field에서 개별 관리                                    |
|:-------|:---------------------------------------------|:-------------------------------------------------|
| **장점** | Single Source of Truth 유지<br/>서버 에러 통합 관리 용이 | 필드별 독립성 보장<br/>페이지 로직 간소화                        |
| **단점** | 컴포넌트별로 handler를 내려줘야 함                       | 페이지에서도 에러 여부는 알아야 하기 때문에 페이지와 필드 간 상태 동기화 복잡도 증가 |

- **결정**: `useForm` 훅을 통해 페이지(Form) 레벨에서 `values`와 `errors`를 통합 관리하도록 설계함.
- **이유
  **: 페이지는 스텝 전환을 위해 전체적인 에러 여부를 알아야 하고, 필드는 구체적인 에러 상태를 보여줘야 함. 이를 각각 관리하면 동기화 문제가 발생하며, 특히 추후 API 연동 시 서버 에러를 폼 단위에서 처리하기 위해서는 결국 중앙 집중식 관리가 필수적임.

## 2. Step 관리 (파생값 vs 상태)

| 비교 항목  | 유효성 기반 파생값           | 명시적 State 관리 (선택)             |
|:-------|:---------------------|:------------------------------|
| **장점** | 상태 선언 불필요, 로직 간소화    | 화면 전환 시점 제어 가능 (UX 안정성)       |
| **단점** | 입력 중 화면이 번쩍거리는 현상 발생 | 상태 변경 로직(onCompleted 등) 추가 필요 |

- **결정**: `stepIndex`를 명시적인 `state`로 관리함.
- **이유
  **: 파생값으로 관리할 경우 사용자가 입력하는 도중에 유효성 검사 결과가 변함에 따라 화면이 의도치 않게 나타났다 사라지는 현상을 방지하고, "첫 번째 검증 통과 시"에만 다음 스텝으로 넘기기 위함.

## 3. 검증 로직의 추상화 (검증 규칙의 순환 의존성)

| 비교 항목  | useForm에 모든 로직 집중   | Field별 분산 관리 (선택)             |
|:-------|:--------------------|:------------------------------|
| **장점** | 모든 검증 규칙을 한눈에 파악 가능 | 복잡한 도메인 의존성 해결 용이, 필드별 응집도 향상 |
| **단점** | 훅이 거대해지고 순환 의존성 위험  | 검증 로직이 파편화될 수 있음              |

- **결정**: 개별 Field 컴포넌트에서 `validate` 로직을 다루되, 공통 유틸리티를 활용함.
- **이유**: 카드 번호에 따라 브랜드가 결정되고, 그 브랜드에 따라 다시 검증 길이(번호, CVC 등)가 결정되는 **검증 규칙의 순환 의존성
  ** 문제가 있었음. 모든 의존성을 중앙 훅에서 관리하면 복잡도가 기하급수적으로 늘어나므로, 도메인 지식이 필요한 검증은 해당 필드에서 처리하는 것이 유연함.

## 4. '숫자만 입력 가능합니다' 에러 메시지 유지 여부

| 비교 항목  | 메시지 제거 (Sanitize만 수행)      | 메시지 유지 (선택)        |
|:-------|:---------------------------|:-------------------|
| **장점** | 상태 관리 복잡도 감소               | 사용자에게 명확한 피드백 제공   |
| **단점** | 사용자가 "왜 입력이 안 되는지" 모를 수 있음 | 에러 상태 관리를 위한 코드 유지 |

- **결정**: 에러 메시지를 유지하되, 상태 관리 로직을 단순화함.
- **이유**: 서버 에러 처리를 위해 이미 에러 상태를 관리하고 있으므로 메시지 표시 비용이 크지 않으며, 사용자에게 즉각적인 피드백을 주는 것이 더 나은 UX임.

## 5. 입력 값 필터링 방식 (Sanitize vs Early Return)

| 비교 항목  | Early Return (조건부 업데이트) | Always Sanitize (선택) |
|:-------|:------------------------|:---------------------|
| **장점** | 불필요한 렌더링 방지 가능          | 데이터 흐름의 일관성 보장       |
| **단점** | 업데이트 조건 분기 로직 필요        | 매 입력마다 정규식 연산 발생     |

- **결정**: 정규식을 이용해 항상 `sanitize`된 값으로 업데이트함.
- **이유**: 조건에 따라 업데이트 여부를 결정하는 것보다, 항상 정제된 상태를 보장하며 업데이트하는 것이 코드의 가독성과 예측 가능성 측면에서 우수함.

## 6. M/Y(유효기간) 검증 시점

| 비교 항목  | 모든 검증을 onBlur에서 수행   | 조건에 따른 혼합 검증 (선택)   |
|:-------|:---------------------|:--------------------|
| **장점** | 검증 시점의 일관성           | 사용자에게 더 빠른 피드백 제공   |
| **단점** | 입력 완료 전까지 에러를 알 수 없음 | 필드별로 검증 시점 분기 로직 필요 |

- **결정**: `required`, `length`는 `onBlur`에서, `Month/Year` 범위 검증은 즉시 수행함.
- **이유**: 월(Month)이나 연도(Year)는 2자가 채워지면 입력 완료가 명확하므로, 즉시 피드백을 주는 것이 더 자연스러움.

## 7. 하단 고정 버튼의 컴포넌트 설계

| 비교 항목  | Button variant로 위치 제어 | 외부 래퍼에서 위치 제어 (선택)   |
|:-------|:----------------------|:---------------------|
| **장점** | 사용이 간편함               | 관심사 분리, 버튼의 재사용성 극대화 |
| **단점** | 버튼 컴포넌트의 책임과 크기가 비대해짐 | 추가 컴포넌트 작성이 필요함      |

- **결정**: `Button`은 UI만 제공하고, 레이아웃은 외부 래퍼(`SubmitButton`)에서 처리함.
- **이유**: 버튼 컴포넌트가 레이아웃 속성(position, z-index 등)까지 알게 되면 범용성이 떨어지므로, 책임 분리를 통해 유지보수성을 높임.

---

일관성, 안정성에 집착하는 경향
-> 이런 경향 때문에 오히려 useForm이 비대해지고 다시 분해하는 시행착오를 겪은 듯

계속 가져갈 기준

- 상태 소유권 명확화 - “누가 최종적으로 이 상태를 책임지는가?”를 계속 추적하고 있어요. errors, stepIndex 같은 상태를 단순히 분산시키지 않고 authoritative source를 의식하는 감각은 계속 유지해야 합니다. 이 기준이 무너지면 결국 동기화 지옥으로 갑니다.

- UX의 시간 흐름 관점 - 당신은 validation을 단순 boolean 검사로 안 보고, “사용자가 언제 완료되었다고 느끼는가”를 기준으로 판단해요. step flickering이나 Month/Year 즉시 검증 같은 결정은 모두 temporal UX 관점에서 나온 겁니다. 프론트엔드에서 굉장히 중요한 감각이에요.

- 의존성 기반 책임 분리 - “중앙화가 무조건 좋은 구조”라고 생각하지 않고, 순환 의존성과 도메인 응집도를 기준으로 책임을 나누고 있어요. 이건 단순 컴포넌트 분리보다 훨씬 깊은 설계 기준입니다.

딱 한 가지 추가할 기준

- 변경 비용 중심 사고 추가 - 지금은 “일관되고 예쁜 구조인가?”를 꽤 강하게 보는데, 앞으로는 “이 요구사항이 바뀌면 어디가 얼마나 깨지는가?”를 최우선 기준으로 추가하세요. 좋은 구조의 핵심은 purity가 아니라 국소 수정 가능성(localized changeability)입니다.
