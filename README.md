# 💳 React Payments

카드 결제 정보를 입력하고 실시간으로 카드 프리뷰를 확인할 수 있는 React 애플리케이션입니다.

## 🎯 학습 목표

- 재사용 가능한 Input Component 개발
- Storybook을 활용한 컴포넌트 상태별 시각적 테스트
- 카드 정보 렌더링을 위한 상태 관리 경험

## 📋 기능 요구사항

### 카드 번호 입력 및 브랜드 식별

| 브랜드     | 식별 규칙                        |
| ---------- | -------------------------------- |
| Visa       | `4`로 시작하는 16자리 숫자       |
| MasterCard | `51`~`55`로 시작하는 16자리 숫자 |

- 숫자(0~9)만 입력 가능
- 4자리씩 4개의 input으로 구성
- 하나의 input에 4자리가 모두 입력되면 카드 프리뷰를 업데이트한다
- 유효하지 않은 번호 입력 시 에러 피드백 제공

### 유효기간 입력

- 월(MM): `01`~`12` 범위만 허용
- 년(YY): `00`~`99` 범위 허용
- 숫자만 입력 가능
- 하나의 input에 2자리가 모두 입력되면 카드 프리뷰를 업데이트한다

### CVC 입력

- `000`~`999` 범위의 3자리 숫자
- 3자리 입력이 완료되어도 카드 프리뷰를 업데이트하지 않는다

### 실시간 프리뷰

- 카드 번호, 유효기간 입력 시 카드 프리뷰가 동시에 업데이트된다
- CVC는 프리뷰에 반영하지 않는다

## 🔧 기술적 의사결정

### 스타일링: Emotion

CSS-in-JS 라이브러리 중 Emotion을 선택했다.

- 카드 프리뷰에 동적으로 변경되는 정보(번호, 브랜드, 유효기간)를 반영할 때 props 기반 스타일링이 직관적이다
- CSS Module 대비: 렌더링되는 styled 컴포넌트가 수백 개 이하 규모이므로 런타임 성능 차이는 무시할 수 있는 수준이다

### 에러 메시지 상수화

에러 메시지를 상수로 분리하여 관리한다.

```
constants/
  errorMessages.ts
```

- 동일한 에러 메시지의 중복 선언 방지
- 메시지 변경 시 단일 지점에서 수정 가능

### 입력 제한 전략

`<input type="number">`가 아닌, `type="text"`에 `inputMode="numeric"`을 조합하고 onChange 핸들러에서 숫자 외 입력을 필터링하는 방식을 고려한다.

> `type="number"`는 `e`, `+`, `-`, `.` 등의 입력을 허용하고, 앞자리 `0` 보존이 안 되는 등의 문제가 있어 카드 입력에는 적합하지 않을 수 있다.

### 구현 가이드

#### step1

- 카드 정보 컨포넌트 제작
- 카드 정보 입력 컨포넌트 제작
- 카드 정보 컨포넌트를 재사용해서 유효기간과 cvc 컨포넌트 제작
- 상단의 카드 프리뷰 컨포넌트 제작
  - 입력한 카드 정보에 따라 카드 프리뷰 리렌더링
    - 카드 정보 숫자의 16자리 중, 처음 8자리만 숫자로 표시되게 한다. 이후 나오는 숫자는 `*`로 처리한다.
    - 유효 기간 정보의 달과 년도는 `달 / 년도`로 표시되게 한다.
    - cvc 번호는 카드 프리뷰에 표시하지 않는다.
- validation 컨포넌트 제작
  - 공통 컴포넌트 부분을 고려하고 제작

#### step2

---

전략

한 컴포넌트 안에는 하나의 역할만 한다.  
 ~~hook은 먼저 컴포넌트 안에서 완성한 뒤, 중복을 확인하고 추출한다.~~

---

구현 순서

1. 카드사 선택 컴포넌트 작성

- 공통 컴포넌트(FormField)를 input과 드롭다운 두 방식에서 어떻게 재사용할지 먼저 고민
- 카드사 선택 토글 UI 작성
- Storybook으로 UI 확인

2. 카드사 토글 상태에 따라 cardPreview 색상 변경

- 토글 선택 → cardPreview 색상 렌더링 확인
- Storybook으로 UI 확인

~~3. 카드 번호 입력 컴포넌트 작성~~

- 4자리 입력 완료 시 다음 input으로 자동 포커스 이동
- 카드 번호 앞자리로 카드사 자동 식별 (Visa / Mastercard / AMEX / Diners / UnionPay)
- 카드사별 번호 길이 유효성 검사
- ~~Storybook으로 UI 확인~~

~~4. 유효기간 / CVC 컴포넌트 작성~~

- ~~숫자만 입력 가능, 유효하지 않은 값 입력 시 에러 피드백~~
- ~~Storybook으로 UI 확인~~

5. 비밀번호 컴포넌트 작성

- 숫자만 입력 가능, 유효하지 않은 값 입력 시 에러 피드백
- Storybook으로 UI 확인

6. 전체 폼 조건부 렌더링 연결

- 카드 번호 전부 채워짐 → 카드사 토글 노출
- 카드사 선택 → 유효기간 노출
- 유효기간 완성 + 에러 없음 → CVC 노출
- CVC 완성 + 에러 없음 → 비밀번호 노출
- 비밀번호 완성 + 에러 없음 → 확인 버튼 활성화
- 이후 값 수정으로 유효하지 않으면 → 확인 버튼 비활성화

7. 확인 버튼 클릭 → /complete 라우팅

- react-router로 페이지 이동

8. 카드 등록 완료 페이지 작성

- 체크 이미지 + **\*로 시작하는 **카드가 등록되었어요 출력
- 확인 버튼 클릭 시 /로 복귀

~~9. hook 추출~~

- ~~중복 로직 확인 후 Form 관리 로직을 hook으로 분리~~
